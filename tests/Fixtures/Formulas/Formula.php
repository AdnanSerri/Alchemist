<?php

namespace App\Formulas;

/**
 * Mirrors the published app/Formulas/Formula.php stub. Lives in the App\Formulas
 * namespace (classmap-autoloaded) because HasAlchemyFormulas hardcodes that
 * namespace when resolving fallback formulas.
 */
class Formula
{
    const BlankParchment = ['id'];
}
